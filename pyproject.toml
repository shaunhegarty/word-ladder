[project]
name = "word-ladder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.4.1",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.6.9",
]
